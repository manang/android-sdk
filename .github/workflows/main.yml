name: Build and Deploy
on:
  schedule:
    - cron:  '* * * * *'  
  push:
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@master

      - name: 'save variable'    
        shell: bash
        run: |
          echo "--> $ENV_VAR_NAME" && ENV_VAR_NAME=$((ENV_VAR_NAME+1)) && echo ::set-env name=ENV_VAR_NAME::"1"
      - name: "read variable"
        shell: bash
        run: |
          echo "--> $ENV_VAR_NAME"
